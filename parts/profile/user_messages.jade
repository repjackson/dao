template(name='user_messages')
    //- .ui.center.aligned.header 
    //-     +icon name='chat'
    //-     |user messages
    if currentUser
        .ui.fluid.icon.big.input
            i.plus.link.submit_message
            input.new_message(type='text' placeholder='new message...')
    .ui.small.segments
        each user_messages
            .ui.segment.raw
                .ui.small.inline.header #{body}
                // div
                // +user_list_toggle key='read_by_ids' label='mark read' icon='eye'
                if can_edit
                    +remove_icon
                //- div
                +author_info
                +creation_info
                +voting_small
