template(name='post_card')
    .ui.card.pointer.post_item
        .content
            .right.floated
                |#{price}p
            a.header(href="/post/#{_id}/view")
                i.store.icon
                |#{title}
                // if private  
                //     i.lock.icon
                
        if image_id
            a.image(href="/post/#{_id}/view")
                img.ui.image(src="{{c.url image_id width=400 height=300 crop='fill'}}")    
        .content
            .meta
                |by 
                if seller.profile_image_id
                    img.ui.avatar.compact.image(src="{{c.url seller.profile_image_id height=200 width=200 gravity='face' crop='fill'}}")
                |#{seller.username}
                // .ui.label #{when}
    
            .description
                |!{blurb}
                // +comments_view_small    
    // .ui.divider

template(name='post_view')
    with current_doc
        .ui.stackable.padded.grid
            .row 
                .four.wide.column
                    .ui.inline.header 
                        small post: 
                    .ui.inline.large.header 
                        |#{title}
                        if private  
                            i.lock.icon
                        
                    if can_edit
                        a.ui.circular.icon.button.edit_post(href="/post/#{_id}/edit")
                            i.large.pencil.icon
                    if image_id
                        img.ui.rounded.image(src="{{c.url image_id gravity='face' crop='fit'}}")
                    .ui.small.inline.header posted by
                    a.ui.header(href="/user/#{_author.username}") 
                        img.ui.avatar.image(src="{{c.url _author.profile_image_id height=200 width=200 gravity='face' crop='fill'}}")
                        |#{_author.username}
                    .ui.inline.small.header #{when}
                    +boolean_view key='rental' direct=true
                    +boolean_view key='for_sale' label='for sale' direct=true
                    div
                    +array_view key='location_tags' label='location_tags' direct=true
                    div
                    +voting_full
                .six.wide.column
                    .f13
                        |!{description}
                    // a.ui.header(href="/user/#{target.username}")
                    //     img.ui.avatar.image(src="{{c.url target.profile_image_id height=200 width=200 gravity='face' crop='fill'}}")
                    //     |#{target.username}
                    .spacer
                    i.large.tags.icon
                    each tags 
                        .ui.basic.large.label #{this}
                    .spacer
                    .spacer
                    +comments
                .six.wide.column
                    if is_author
                        .ui.large.fluid.teal.circular.button.log_purchase(class=buy_button_class)
                            i.cart.arrow.down.large.icon
                            |log purchase for #{total_price}p
                    else
                        .ui.large.fluid.green.circular.button.buy(class=buy_button_class)
                            i.cart.arrow.down.large.icon
                            |buy for #{total_price}p
                    .ui.header #{inventory} left
                    
                    .ui.dividing.header 
                        i.list.icon
                        // +icolor name='activity-history' cl='ui inline image'
                        |#{post_purchases.count} sales
                    .ui.link.selection.list.medscroll
                        each orders
                            a.ui.item(href="/order/#{_id}/view") 
                                .content
                                    .header
                                        .ui.inline.header #{purchase_price}c
                                        
                                        |#{when} 
                                        img.ui.avatar.image(src="{{c.url buyer.profile_image_id height=200 width=200 gravity='face' crop='fill'}}")
                                        |#{buyer.username}
                                        .ui.small.header submitted: #{submitted}
                                        .ui.small.header transfer: #{transfer}
                                        if key_value_is 'transfer' 'delivery'
                                            +datetime_view key='delivery_datetime' label='delivery time' direct=true
                                            +textarea_view key='delivery_location' label='delivery location' direct=true
                                            +textarea_view key='delivery_notes' label='delivery notes' direct=true
                                            +text_view key='notes' label='notes' direct=true
                                            +boolean_view key='delivery_started' direct=true
                                            if delivery_started
                                                div
                                                +boolean_view key='delivery_complete' direct=true
                                                if delivery_complete
                                                    +image_view key='delivery_image' direct=true
                                                
                                                    .ui.header customer
                                                    +boolean_view key='delivery_accepted' label='delivery accepted' direct=true                    
                                        if key_value_is 'transfer' 'pickup'
                                            +datetime_view key='pickup_datetime' label='pickup date time' direct=true
                                            +textarea_view key='notes' label='notes' direct=true
    
                                        
                                    // if can_edit 
                                    //     i.
                                    //     +icolor name='return-purchase' cl='ui mini inline image return crediter'
                    

template(name='post_edit')
    with current_doc
        .ui.stackable.padded.grid
            .row 
                .sixteen.wide.column
                    a.ui.green.icon.circular.large.button(href="/post/#{_id}/view" title='save')
                        i.checkmark.big.icon
                    .ui.inline.large.header 
                        small edit post:
                        |#{title}
                        // if private  
                        //     i.lock.icon
                    i.large.remove.link.circular.icon.delete_post(title='delete post')
            .row 
                .four.wide.column
                    .ui.header 
                        small author
                        |#{_author.username}
                    +text_edit key='title' icon='header' direct=true
                    div
                    // +boolean_edit key='private' direct=true
                    // if private
                    //     +multi_user_edit key='viewable_user_ids' key2='viewable_user_usernames' label='viewable by' direct=true
                .eight.wide.column
                    div
                    +html_edit key='description' label='description' direct=true
                    div
                    +boolean_edit key='has_image' label='image' direct=true
                    if has_image
                        +image_edit key='image_id' label='image' direct=true
                    div 
                    +array_edit key='tags' label='tags' icon='tags' direct=true
                    div
                .four.wide.column
                    |coauthors split revenue evenly
                    +multi_user_edit key='coauthor_ids' key2='coauthor_usernames' label='coauthors' direct=true
                    