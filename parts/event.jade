template(name='events')
    .ui.padded.stackable.grid
        .sixteen.wide.column
            // .ui.button 
            //     |debit count
            // .ui.button 
            //     |debit amount
            // .ui.button 
            //     |credit count
            // .ui.button 
            //     |credit amount
            .ui.inline.big.header 
                // i.calendar.big.icon
                +icolor name='calendar'
                span.mobile.hidden events
            if is_admin
                .ui.icon.button.add_event
                    i.plus.big.green.icon
                    // |add event
            .scrolling
                .ui.stackable.six.cards
                    each next_events
                        +event_item
        .sixteen.wide.column
            // +event_cloud


template(name='event_item')
    .ui.card
        if image_id
            a(href="/event/#{_id}/view")
                img.ui.image(src="{{c.url image_id height=200 width=200 gravity='face' crop='fill'}}")
        .content
            a.ui.header(href="/event/#{_id}/view") #{title}
            |#{when}
            |{{from_now start_datetime}}
            |- {{from_now end_datetime}}
        .content
            .ui.basic.green.button.pick_going(title='going')
                i.thumbs.up.outline.icon
                |#{going_user_ids.length}
                each going
                    |#{username}
            .ui.basic.yellow.button.pick_maybe(title='maybe')
                i.minus.icon
                | #{maybe_user_ids.length}
                each maybe_going
                    |#{username}
            .ui.basic.red.button.pick_not(title='not going')
                i.thumbs.down.outline.icon
                | #{not_user_ids.length}
                each not_going
                    | #{username}
        



template(name='event_view')
    with current_doc
        .ui.stackable.padded.grid
            .middle.aligned.row 
                .four.wide.center.aligned.column
                    a.ui.big.circular.button(href="/events")
                        i.chevron.left.icon
                        +icolor name='calendar' cl='ui inline image'
                        span.mobile.hidden events
                .eight.wide.center.aligned.column
                    .ui.small.inline.header(title='viewing event')
                        +icolor name='tear-off-calendar'
                        // i.calendar.icon
                        // |view event
                    .ui.large.inline.header #{title}
                    div
                    .ui.inline.header {{long_date start_datetime}}
                    .ui.inline.grey.header {{from_now start_datetime}}
                    
                .four.wide.center.aligned.column
                    if can_edit
                        a.ui.circular.icon.button.edit_event(href="/event/#{_id}/edit" title='edit')
                            i.big.blue.pencil.icon

            .row 
                .four.wide.column
                    .ui.segment
                        .ui.header 
                            small author
                        a.ui.header(href="/user/#{_author.username}") 
                            img.ui.avatar.image(src="{{c.url _author.profile_image_id height=200 width=200 gravity='face' crop='fill'}}")
                            |#{_author.username}
                    .ui.header
                        |#{max_attendees} max attendees
                    .ui.segment
                        .ui.inline.header 
                            +icolor name='event-accepted'
                            |#{going.count} going
                        .ui.icon.circular.button.pick_going 
                            i.green.checkmark.big.icon
                        .ui.big.list
                            each going 
                                a.ui.circular.button(href="/user/#{username}")    
                                    img.ui.avatar.compact.image(src="{{c.url currentUser.profile_image_id height=200 width=200 gravity='face' crop='fill'}}")
                                    |#{username}
                    
                    .ui.segment
                        .ui.inline.header 
                            +icolor name='event-accepted-tentatively'
                            |#{maybe.count} maybe
                        .ui.icon.circular.button.pick_maybe 
                            i.yellow.minus.big.icon
                        .ui.big.list
                            each maybe_going
                                a.ui.circular.button(href="/user/#{username}")    
                                    img.ui.avatar.compact.image(src="{{c.url currentUser.profile_image_id height=200 width=200 gravity='face' crop='fill'}}")
                                    |#{username}
                    .ui.segment
                        .ui.inline.header 
                            +icolor name='event-declined'
                            |#{not_going.count} not going
                        .ui.circular.icon.button.pick_not
                            i.red.big.ban.icon
                        .ui.big.list
                            each not_going
                                a.ui.circular.button(href="/user/#{username}")    
                                    img.ui.avatar.compact.image(src="{{c.url currentUser.profile_image_id height=200 width=200 gravity='face' crop='fill'}}")
                                    |#{username}
                            
                    .ui.small.grey.header 
                        i.line.chart.icon
                        |stats
                    .ui.list
                        .item #{views} views
                    each viewers
                        .ui.label #{username
                    .ui.hidden.divider
                .eight.wide.column
                    +image_view key='image_id' label='image' direct=true
                    // a.ui.header(href="/user/#{target.username}")
                    //     img.ui.avatar.image(src="{{c.url target.profile_image_id height=200 width=200 gravity='face' crop='fill'}}")
                    //     |#{target.username}
                    .scrolling
                        +textarea_view key='description' label='description' direct=true
                    div 
                    +array_view key='tags' label='tags' direct=true
                    .spacer
                    .spacer
                    +comments
                    // +voting_full
                    //- +array_view key='location_tags' label='location_tags' direct=true
                .four.wide.column
                    .ui.scondary.segment
                        .ui.inline.header 
                            // i.cart.icon
                            +icolor name='buy'
                            |buy ticket
                        .ui.grey.small.inline.header #{tickets_left} left
                        div
                        .ui.big.circular.green.button.buy_for_usd
                            // |buy for 
                            // +icolor name='money' cl='ui inline image'
                            |$#{usd_price}
                        .ui.inline.header or
                        .ui.big.circular.teal.button.buy_for_points
                            // i.cart.icon
                            // |buy for 
                            |#{point_price}pts
                        .ui.header your points #{currentUser.points}
                
                    .ui.header 
                        +icolor name='activity-history' cl='ui inline image'
                        |#{ticket.length} purchased tickets
                    .ui.large.link.selection.list
                        each tickets
                            a.ui.item(href="/ticket/#{_id}/view") 
                                .content
                                    .header
                                        if is_usd
                                            .ui.inline.green.header ${{money_format amount}}
                                        else
                                            .ui.inline.header #{point_amount}pts
                                        
                                        |#{when} #{_author_username}
                                    if can_edit 
                                        .ui.circular.icon.button.return
                                            +icolor name='return-purchase' cl='ui mini inline image'
                                    if is_admin
                                        +remove_button

                    //- .ui.fluid.green.button.submit
                    //-     i.checkmark.icon
                    //-     |submit

template(name='event_edit')
    with current_doc
        .ui.stackable.padded.grid
            .centered.middle.aligned.row 
                .four.wide.center.aligned.column
                    a.ui.circular.icon.green.big.button(href="/event/#{_id}/view" title='save')
                        i.checkmark.big.icon
                .eight.wide.center.aligned.column
                    .ui.header 
                        i.calendar.icon
                        
                        |edit event
                .four.wide.center.aligned.column
                    .ui.red.circular.icon.button.delete_item(title='delete')
                        i.large.remove.icon
            .three.column.row
                .column
                    .ui.header 
                        small author
                        |#{_author.username}
                    +datetime_edit key='start_datetime' label='start' direct=true
                    +datetime_edit key='end_datetime' label='end' direct=true
                .column
                    +text_edit key='title' label='title' direct=true
                    .ui.header
                        |audience
                    +number_edit key='max_attendees' label='max attendees' direct=true
                    +number_edit key='point_price' label='points per ticket' direct=true
                    // +key_value_edit key='point_price' value=1
                    // +key_value_edit key='point_price' value=5
                    // +key_value_edit key='point_price' value=10
                    // +key_value_edit key='point_price' value=20
                    +number_edit key='usd_price' label='dollars per ticket' direct=true
                .column
                    +textarea_edit key='description' label='description' direct=true
                    +image_edit key='image_id' label='image' direct=true
                    div 
                    +array_edit key='tags' label='tags' direct=true
                    //- +array_edit key='location_tags' label='location_tags' direct=true
                    // unless published
                    //     a.ui.fluid.green.button.submit(href="/save")
                    //         i.checkmark.icon
                    //         |publish
                    // else
                    //     .ui.basic.button.unpublish 
                    //         |unpublish
